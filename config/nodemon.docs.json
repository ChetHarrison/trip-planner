{
  "watch": ["public/js", "server"],
  "ext": "js",
  "exec": "npm run docs",
  "ignore": [
    "diagnostics/docs/**",        // ignore the new path
    "diagnostics/coverage/**",    // keep ignoring coverage
    "diagnostics/test-report/**"  // optional: tests
  ]
}